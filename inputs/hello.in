// The program input for hello/src/main.leo
[main]
public a: u32 = 1u32;
b: u32 = 2u32;

// When you use the `leo run subtract` command, 
// it will take these values as inputs for the program. 
// Since we have declared the function with the parameter data type as i32,
// make sure to follow the same data type when providing the input.
[substract]
public a: i32 = 4i32;
b: i32 = 2i32;